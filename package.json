{
  "name": "ai-bible-translator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "translate": "dotnet run -- modern",
    "resubmit": "dotnet run -- failed",
    "format": "dotnet format AI-Bible-Translator.sln",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6"
  },
  "lint-staged": {
    "*.cs": [
      "echo 'Running dotnet format on AI-Bible-Translator.sln…'",
      "dotnet format AI-Bible-Translator.sln --include"
    ]
  }
}
